{
    "name": "yelp-clone",
    "license": "MIT",
    "engines": {
      "node": "10.17.0",
      "yarn": "1.15.2"
    },
    "scripts": {
      "build": "yarn --cwd src install && yarn --cwd crs build",
      "deploy": "cp -a src/build/. public/",
      "heroku-postbuild": "yarn build && yarn deploy"
    }
  }